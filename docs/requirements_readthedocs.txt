# Read the Docs installs these (see .readthedocs.yaml). Kept in sync with the
# [project.optional-dependencies] "docs" extra in pyproject.toml, plus tensorflow,
# which the root __init__.py imports at import time (so autodoc/version resolution needs it).
sphinx==5.0.2
myst-parser==0.18.0
docutils<0.18
sphinx_rtd_theme==1.0.0
sphinxcontrib-bibtex==2.4.2
pybtex==0.24.0
setuptools<81
tensorflow==2.15.1